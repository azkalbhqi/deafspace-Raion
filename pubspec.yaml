name: deafspace_prod
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  google_fonts: ^6.2.1
  fluttertoast: ^8.2.8
  flutter_tts: ^4.0.2
  speech_to_text: ^6.2.1
  geocoding: ^3.0.0
  google_maps_flutter: ^2.9.0
  location: ^7.0.0
  # flutter_polyline_points: ^1.0.0
  cloud_firestore: ^5.3.0
  intl: ^0.19.0
  http: ^1.2.2
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/images/logos/logo.png
  - assets/images/img/1.png    # Ensure correct indentation for assets
  - assets/images/img/2.png    # Ensure correct indentation for assets
  - assets/images/img/3.png    # Ensure correct indentation for assets
  - assets/images/img/4.png    # Ensure correct indentation for assets
